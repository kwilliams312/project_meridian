# Seed skeleton pack (SP1.8 integration proof) — a self buff that raises stamina.
# Exercises the `buff` effect primitive whose `attribute` field is L011-resolved
# against the meridian/attribute@1 catalog (since #652).
schema: meridian/ability@1
id: core:ability.iron_resolve
name: Iron Resolve
description: Steels the body, increasing stamina for a time.
school: physical
target: self
range_m: 0
cast:
  time_ms: 0
cooldown_ms: 45000
triggers_gcd: false
resource:
  type: rage
  amount: 10
effects:
  - kind: buff
    attribute: core:attribute.stamina
    amount: 8
    modifier: flat
    duration_ms: 12000
