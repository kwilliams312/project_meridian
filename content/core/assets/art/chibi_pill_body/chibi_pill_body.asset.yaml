# IF-8 sidecar (meridian/asset@1). Chibi pill-body base — the keystone SP5 chibi
# character base (epic #722): a real stylized low-poly mesh generated with Meshy
# (text-to-3D, model meshy-5) and restyled to the canonical Meridian rig per the
# tools/blender/README.md restyle procedure (spec ⑤/S4) under the new `chibi`
# proportion profile (story #745): fitted to rig scale + T-pose stance,
# budget-cleaned to the Art PRD §2.1 player-body band with an authored LOD0-3
# chain, cut into the 8 geo_<region>_lod<N> geosets, and skinned to the
# canonical 63-bone armature (<=4 influences, normalized). The chibi profile
# tucks the arm/hand/leg joints inside the compact pill so all 8 geosets are cut
# from real sculpt geometry — no proxy-box fallback (the floating-bar artifact
# that a human ardent_male restyle of this pill produced). source_tier: ai — the
# restyle pass is complete, so restyle_status: done (the #525 quarantine lint
# blocks 'pending').
schema: meridian/asset@1
id: core:art.chibi_pill_body
class: character_model
source: assets/art/chibi_pill_body/sk_chibi_pill_body.glb
license: CC-BY-4.0
provenance:
  source_tier: ai
  authors: [meridian-contributors]
  origin_url: https://api.meshy.ai/openapi/v2/text-to-3d/019f61e8-2dde-7250-a9cc-a6c720ec47f9
  ai:
    tool: meshy@meshy-5
    prompts_file: chibi_pill_body.prompts.yaml
budget:
  # 8 geoset regions × LOD0, summed. Inside the 45-60k player-body band, under
  # the 60k character_model ceiling (validate_content L070).
  lod0_tris: 51999
import_hints:
  # Real body ships an authored LOD0-3 chain per geoset region (I023 — no
  # 'single' exemption, which only the greybox blockout used).
  lod_policy: authored
restyle_status: done
