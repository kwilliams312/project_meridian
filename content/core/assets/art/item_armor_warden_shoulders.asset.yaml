# IF-8 sidecar (meridian/asset@1). Warden's Kit PAULDRONS — a real Meshy
# text-to-3D plate sculpt (model meshy-5) restyled to the canonical rig via
# tools/blender/meridian_rig/restyle_armor.py (--region shoulders, spec ⑤/S4, epic
# #594): fitted to the real body torso surface (geo_torso_lod0) inflated a hair
# outward so it sits just proud of the body with no void, decimated to the Art PRD
# §2.1 armor band (6,000 tris), given the deterministic cylindrical dye UV normalized
# against the fitted bounds so the existing warden_shoulders_mask.png still tints it, its
# Meshy photo-texture stripped for the flat light-steel dye base, and single-influence
# skinned to the canonical LeftShoulder/RightShoulder bone(s). Replaces the procedural blockout at the SAME
# asset id (a content swap; idmap id + golden + staged pack regenerated). source_tier:
# ai — the restyle pass is complete, so restyle_status: done (#525 quarantine lint
# blocks 'pending'). Rolls out the proven chest pipeline (#596) to the shoulders slot (#597).
schema: meridian/asset@1
id: core:art.item.armor.warden_shoulders
class: armor_model
source: assets/art/item/armor/warden_shoulders.glb
license: CC-BY-4.0
provenance:
  source_tier: ai
  authors: [meridian-contributors]
  origin_url: https://api.meshy.ai/openapi/v2/text-to-3d/019f574c-d983-7d3c-8d10-812f2b687ceb
  ai:
    tool: meshy@meshy-5
    prompts_file: item_armor_warden_shoulders.prompts.yaml
budget:
  # Single armor mesh, decimated into the Art PRD §2.1 3-8k armor band, under the
  # 8k armor_model ceiling (validate_content L070).
  lod0_tris: 6000
import_hints:
  # Gear binds a SUBSET of canonical bones and ships a single mesh (no forced LOD
  # chain — I023's authored-chain rule is character_model-only).
  lod_policy: single
restyle_status: done
