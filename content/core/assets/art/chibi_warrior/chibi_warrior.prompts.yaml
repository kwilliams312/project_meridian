# Auxiliary prompts/provenance companion for core:art.chibi_warrior (story #725).
# Not a content entity — validate_content.py + mcc discovery exclude *.prompts.yaml
# from envelope checks (spec ④ §7.2, issue #410 precedent). Referenced by
# chibi_warrior.asset.yaml -> provenance.ai.prompts_file (Art PRD §3.2 prompt hygiene).
schema: meridian/meshy-prompts@1
tool: meshy@meshy-5
generation_mode: operator_supplied  # generated by the operator in the Meshy web UI, not via `python -m meshy`
operator: kwilliams312
generated_on: "2026-07-13"
# The exact prompt string was entered in the Meshy web UI and not captured verbatim
# at intake. Recorded here: the intent + the mandatory prompt-hygiene attestation.
prompt: >-
  Operator-supplied Meshy generation of a chibi warrior in the locked "colored
  pills" style — a bright pill-shaped body with a face, wearing armor.
prompt_hygiene_attested: true  # no franchise / brand / studio / living-artist terms (Art PRD §3.2)
task_ids: []  # no Meshy API refine-task id — manual web-UI generation, not the CLI path
notes: >-
  Raw Meshy output: static, un-rigged mesh + one baked 4096 RGBA texture. Landed as
  an intake candidate. restyle_status is 'pending' (L024 quarantine, expected). The
  canonical-skeleton rig conversion (tools/meshy convert-rig, spec ④ §7.3), the
  restyle pass, and LODs are follow-on human/art work — see the PR.
