# IF-8 sidecar (meridian/asset@1). Ardent male base body — a real stylized
# low-poly mesh generated with Meshy (text-to-3D, model meshy-5) and restyled to
# the canonical Meridian rig per the tools/blender/README.md restyle procedure
# (spec ⑤/S4): fitted to rig scale + T-pose stance, budget-cleaned to the Art
# PRD §2.1 player-body band with an authored LOD0-3 chain, cut into the 8
# geo_<region>_lod<N> geosets, and skinned to the canonical 63-bone armature
# (<=4 influences, normalized). Replaces the greybox blockout at the SAME asset
# id (a content swap; idmap id 81 is reused, golden + staged pack regenerated).
# source_tier: ai — the restyle pass is complete, so restyle_status: done (the
# #525 quarantine lint blocks 'pending').
schema: meridian/asset@1
id: core:art.char.ardent.male.base
class: character_model
source: assets/art/char/sk_ardent_male_base.glb
license: CC-BY-4.0
provenance:
  source_tier: ai
  authors: [meridian-contributors]
  origin_url: https://api.meshy.ai/openapi/v2/text-to-3d/019f54a3-7605-7e13-8024-ad5cc061a91b
  ai:
    tool: meshy@meshy-5
    prompts_file: ardent_male_base.prompts.yaml
budget:
  # 8 geoset regions × LOD0, summed. Inside the 45-60k player-body band, under
  # the 60k character_model ceiling (validate_content L070).
  lod0_tris: 51998
import_hints:
  # Real body ships an authored LOD0-3 chain per geoset region (I023 — no
  # 'single' exemption, which only the greybox blockout used).
  lod_policy: authored
restyle_status: done
