# IF-8 sidecar (meridian/asset@1). Ardent male hair preset 3 — a warrior top-knot
# with shaved undercut sides. A real stylized low-poly mesh generated with Meshy
# (text-to-3D, model meshy-5) and restyled to the canonical rig's HEAD region per
# tools/blender/README.md (spec ⑤/S5): fitted onto the head geoset span,
# budget-cleaned to a light low-poly shell, single-influence skinned to the
# canonical Head bone (weight 1.0/vertex) on the shared armature, and exported
# through the meridian_export E100-E105 gate.
#
# class: armor_model — hair is a skinned piece bound to a SUBSET of canonical
# bones (just Head), which the client assembler mounts via the same
# _mount_skinned gear path. armor_model is the class whose E/I rules describe
# exactly this asset: E100 (canonical bones) + E104 (no geoset naming) + E103
# (single influence) apply; the body-only E101/E102 and the character_model
# I020/I022 skip. lod_policy: single opts out of the LOD-chain rule I023 — hair
# is a single light mesh, not a full body.
#
# source_tier: ai — the restyle pass is complete, so restyle_status: done (the
# #525 quarantine lint blocks 'pending').
schema: meridian/asset@1
id: core:art.char.ardent.male.hair_3
class: armor_model
source: assets/art/char/ardent/male/hair_3.glb
license: CC-BY-4.0
provenance:
  source_tier: ai
  authors: [meridian-contributors]
  origin_url: https://api.meshy.ai/openapi/v2/text-to-3d/019f54d6-4662-7726-8a1d-a1183aaa6dcb
  ai:
    tool: meshy@meshy-5
    prompts_file: hair_3.prompts.yaml
budget:
  lod0_tris: 2500
import_hints:
  lod_policy: single
restyle_status: done
