# IF-8 sidecar (meridian/asset@1). Warden's Kit CHEST cuirass — a real Meshy
# text-to-3D plate sculpt (model meshy-5) restyled to the canonical rig via
# tools/blender/meridian_rig/restyle_armor.py (spec ⑤/S4, #595): fitted to the
# real body torso surface (geo_torso_lod0) inflated a hair outward so it sits just
# proud of the body with no void and reaches up over the shoulder girdle to bridge
# the neck/shoulder seam, decimated to the Art PRD §2.1 armor band (6,000 tris),
# given the deterministic cylindrical dye UV so the existing warden_chest_mask.png
# still tints it, its Meshy photo-texture stripped for the flat light-steel dye
# base, and single-influence skinned to the canonical Spine/Chest/UpperChest column.
# Replaces the procedural blockout at the SAME asset id (a content swap; idmap id +
# golden + staged pack regenerated). source_tier: ai — the restyle pass is complete,
# so restyle_status: done (the #525 quarantine lint blocks 'pending'). PROVES the
# Meshy-armor pipeline (epic #594); the other 5 slots follow after the GPU render.
schema: meridian/asset@1
id: core:art.item.armor.warden_chest
class: armor_model
source: assets/art/item/armor/warden_chest.glb
license: CC-BY-4.0
provenance:
  source_tier: ai
  authors: [meridian-contributors]
  origin_url: https://api.meshy.ai/openapi/v2/text-to-3d/019f5722-1571-7cd2-9b96-dd03f7366cf6
  ai:
    tool: meshy@meshy-5
    prompts_file: item_armor_warden_chest.prompts.yaml
budget:
  # Single armor mesh, decimated into the Art PRD §2.1 3–8k armor band, under the
  # 8k armor_model ceiling (validate_content L070).
  lod0_tris: 6000
import_hints:
  # Gear binds a SUBSET of canonical bones and ships a single mesh (no forced LOD
  # chain — I023's authored-chain rule is character_model-only).
  lod_policy: single
restyle_status: done
