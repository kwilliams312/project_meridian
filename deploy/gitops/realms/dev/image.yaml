# SPDX-License-Identifier: Apache-2.0
# MACHINE-MANAGED by .github/workflows/cd.yml — do NOT edit by hand.
# Immutable image pin for the Dev realm (issue #380): the just-built
# <short-sha>, committed back on every push to dev so ArgoCD sees a
# manifest diff and drives the rollout declaratively. Overrides the
# moving :dev tag from realms/dev/values.yaml (loaded before this file).
image:
  tag: "ed9f0f0"
mariadb:
  image:
    tag: "ed9f0f0"
seed:
  accountImage:
    tag: "ed9f0f0"
